Added background music. (Game.music = true);
Added particle effects trail to ship. (Game.particles = true);
If debug mode is enabled, bounding boxes around all objects are rendered and some on-screen stats are displayed. (Game.debug = false);